{
  "name": "website-cloner",
  "version": "1.0.0",
  "description": "AI-powered website cloner — Phase 1 harvests, Phase 2 uses Antigravity to generate the clone",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install:browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "adm-zip": "^0.5.18",
    "express": "^4.18.2",
    "playwright": "^1.44.0",
    "prettier": "^3.9.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
